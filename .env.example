# =============================================================================
# Environment Variables Configuration
# =============================================================================
# Copy this file to .env and fill in the values.
# All variables are validated at application startup using Zod schemas.
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# PostgreSQL connection string.
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres:postgres@localhost:5432/promptify
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/promptify"

# -----------------------------------------------------------------------------
# Authentication Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Better Auth base URL for authentication callbacks and API endpoints.
# In development, typically: http://localhost:3000
# In production, use your production domain: https://yourdomain.com
BETTER_AUTH_URL="http://localhost:3000"

# Better Auth secret key for signing and encrypting cookies.
# Must be at least 32 characters long.
# Generate a secure secret with: openssl rand -base64 32
# Keep this secret secure and never commit it to version control.
BETTER_AUTH_SECRET="your-awesome-secret-minimum-32-characters-long"

# -----------------------------------------------------------------------------
# AI Service Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# OpenRouter API key for AI chat functionality.
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY="your-openrouter-api-key-here"

# -----------------------------------------------------------------------------
# Logging Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Logging level: 'fatal' | 'error' | 'warn' | 'info' | 'debug' | 'trace'
# Defaults to 'debug' in development, 'info' in production.
# Uncomment to override:
# LOG_LEVEL="info"

# -----------------------------------------------------------------------------
# PII Detection Configuration (OPTIONAL, admin-only)
# -----------------------------------------------------------------------------
# Enable real-time PII detection and masking in streaming AI responses.
# Default: false (disabled)
# PII_DETECTION_ENABLED="false"

# Run PII detection after every N streaming chunks. Smaller = more accurate, higher cost.
# Default: 5
# PII_CHUNK_BATCH_SIZE="5"

# Timeout in ms for PII detection API calls. Default: 5000
# PII_DETECTION_TIMEOUT_MS="5000"

# Model to use for PII detection via OpenRouter. Default: openai/gpt-5-nano
# PII_DETECTION_MODEL="openai/gpt-5-nano"

# Comma-separated PII types to detect. Omit to use all: email, phone, ssn, credit_card, address, full_name, gov_id, ip, dob
# PII_TYPES="email,phone,ssn"

# When detection service is unavailable: continue_without_masking (default) or fail
# PII_FALLBACK_WHEN_UNAVAILABLE="continue_without_masking"

# -----------------------------------------------------------------------------
# Node Environment (OPTIONAL)
# -----------------------------------------------------------------------------
# Node environment: 'development' | 'production' | 'test'
# Usually provided by runtime (Next.js, Node.js), but can be set here.
# Defaults based on runtime environment if not set.
# Uncomment to override:
# NODE_ENV="development"
